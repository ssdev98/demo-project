name: example1 

on:
  push:
    branches: [main]
  #pull_request: 
    #branches: [develop]  

jobs:
  first-job: # job name can be anything
    runs-on: ubuntu-latest #or windows-latest

    steps:
      - run: node --version
      - run: npm --version
